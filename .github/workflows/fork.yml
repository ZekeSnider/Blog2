name: Sync Fork

on:
  push:
    branches:    
      - 'main'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: tgymnich/fork-sync@v1.4
        with:
          base: main
          head: main